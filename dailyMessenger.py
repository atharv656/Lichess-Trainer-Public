import os

# Set the enviornment variable 'Input' so that messages will be sent to all users
os.environ['INPUT'] = "Nice"
# When the bot is launched, it will send a message to all users and then exit the program
import discordBot
